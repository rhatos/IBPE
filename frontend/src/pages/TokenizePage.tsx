import TokenizeMenu from "../components/TokenizeMenu";

const TokenizePage = () => {
  return (
    <div className="flex flex-col justify-center items-center space-y-4">
      {/* Training Form */}
      <TokenizeMenu />
    </div>
  );
};

export default TokenizePage;
